<Window x:Class="ElPerrito.WPF.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="clr-namespace:ElPerrito.WPF"
xmlns:vm="clr-namespace:ElPerrito.WPF.ViewModels"
mc:Ignorable="d"
Title="El Perrito"
Height="600" Width="1000"
WindowStartupLocation="CenterScreen"
WindowState="Maximized">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menú superior -->
        <Grid Grid.Row="0" Background="#2C3E50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,5">
                <Image Source="/Assets/logo.png" Height="30" Width="30" Margin="0,0,10,0" Stretch="Uniform"/>
                <TextBlock Text="El Perrito" FontSize="18" FontWeight="Bold"
                          Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Menú de navegación -->
            <Menu Grid.Column="1" Background="Transparent" Foreground="White" FontSize="14"
                  VerticalAlignment="Center">
                <!-- Sección Cliente -->
                <MenuItem Header="🛒 TIENDA" Command="{Binding ShowShopCommand}"
                          Foreground="White" Padding="10,5" FontWeight="Bold"/>
                <MenuItem Header="🛍️ MI CARRITO" Command="{Binding ShowCartCommand}"
                          Foreground="White" Padding="10,5" FontWeight="Bold"
                          Visibility="{Binding IsCliente, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <Separator Background="#34495E" Margin="5,0"
                          Visibility="{Binding IsAdminOrOperador, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Sección Admin -->
                <MenuItem Header="📦 Productos" Command="{Binding ShowProductsCommand}"
                          Foreground="White" Padding="10,5"
                          Visibility="{Binding IsAdminOrOperador, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <MenuItem Header="💰 Ventas" Command="{Binding ShowSalesCommand}"
                          Foreground="White" Padding="10,5"
                          Visibility="{Binding IsAdminOrOperador, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <MenuItem Header="📊 Reportes" Command="{Binding ShowReportsCommand}"
                          Foreground="White" Padding="10,5"
                          Visibility="{Binding IsAdminOrOperador, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <MenuItem Header="⚙️ Configuración" Command="{Binding ShowSettingsCommand}"
                          Foreground="White" Padding="10,5"
                          Visibility="{Binding IsAdminOrOperador, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Menu>

            <!-- Opciones de usuario (derecha) -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,5">
                <!-- Botón de Iniciar Sesión (solo visible cuando NO está autenticado) -->
                <Button Content="🔑 Iniciar Sesión" Command="{Binding LoginCommand}"
                        Background="#3498DB" Foreground="White"
                        BorderThickness="0" Padding="15,8" Margin="5,0"
                        FontWeight="Bold" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                               CornerRadius="5"
                                               BorderThickness="0"
                                               Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAuthenticated}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2980B9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#21618C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Menú de usuario (solo visible cuando está autenticado) -->
                <Menu Background="Transparent" Foreground="White" FontSize="14"
                      Visibility="{Binding IsAuthenticated, Converter={StaticResource BoolToVisibilityConverter}}">
                    <MenuItem Header="{Binding UserFullName}" Foreground="White" Padding="10,5">
                        <MenuItem Header="Cerrar Sesión" Command="{Binding LogoutCommand}" Foreground="#E74C3C"/>
                    </MenuItem>
                </Menu>

                <!-- Botón Salir -->
                <Button Content="🚪" Command="{Binding ExitCommand}"
                        Background="Transparent" Foreground="White"
                        BorderThickness="0" Padding="10,5"
                        FontSize="16" Cursor="Hand" ToolTip="Salir">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E74C3C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Contenido principal -->
        <Grid Grid.Row="1" Background="#ECF0F1">
            <Border Margin="20" Background="White" CornerRadius="10"
                    BorderBrush="#BDC3C7" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315"
                                    ShadowDepth="5" Opacity="0.3"/>
                </Border.Effect>

                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Título de la vista actual -->
                    <TextBlock Grid.Row="0"
                               Text="{Binding CurrentView}"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="#2C3E50"
                               Margin="0,0,0,20"/>

                    <!-- Contenido dinámico -->
                    <ContentControl Grid.Row="1" Content="{Binding CurrentViewContent}"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
