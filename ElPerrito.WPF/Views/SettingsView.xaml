<UserControl x:Class="ElPerrito.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <!-- Configuración General -->
            <Border Background="White" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="20" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Configuración General" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Nombre de la Aplicación:" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationName}" Height="30" Margin="0,5" Padding="5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Cadena de Conexión:" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ConnectionString}" Height="30" Margin="0,5" Padding="5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Registros por página:" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RecordsPerPage}" Height="30" Margin="0,5" Padding="5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Modo Debug:" VerticalAlignment="Center" Margin="0,5"/>
                        <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding DebugMode}" VerticalAlignment="Center" Margin="0,5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Configuración de Inventario -->
            <Border Background="White" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="20" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Configuración de Inventario" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Stock mínimo por defecto:" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DefaultMinStock}" Height="30" Margin="0,5" Padding="5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Alertas de stock bajo:" VerticalAlignment="Center" Margin="0,5"/>
                        <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding LowStockAlerts}" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Enviar notificaciones por email:" VerticalAlignment="Center" Margin="0,5"/>
                        <CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding EmailNotifications}" VerticalAlignment="Center" Margin="0,5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Configuración de Reportes -->
            <Border Background="White" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="20" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Configuración de Reportes" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ruta de exportación:" VerticalAlignment="Center" Margin="0,5"/>
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
                            <TextBox Text="{Binding ExportPath}" Width="300" Height="30" Padding="5" Margin="0,0,10,0"/>
                            <Button Content="Examinar" Command="{Binding BrowseExportPathCommand}"
                                    Width="100" Height="30"
                                    Background="#3498DB" Foreground="White" FontSize="12" BorderThickness="0" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Formato por defecto:" VerticalAlignment="Center" Margin="0,5"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" SelectedIndex="{Binding DefaultReportFormat}" Width="150" HorizontalAlignment="Left" Height="30" Margin="0,5">
                            <ComboBoxItem Content="PDF"/>
                            <ComboBoxItem Content="Excel"/>
                            <ComboBoxItem Content="CSV"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Información del Sistema -->
            <Border Background="#F8F9FA" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="20" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Información del Sistema" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Versión:" VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="1.0.0" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Framework:" VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text=".NET 8.0" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Base de Datos:" VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="MySQL / MariaDB" VerticalAlignment="Center" Margin="0,5"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Patrones Implementados:" VerticalAlignment="Center" Margin="0,5" FontWeight="Bold"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="20 Patrones de Diseño" VerticalAlignment="Center" Margin="0,5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Botones de acción -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Guardar Cambios" Command="{Binding SaveSettingsCommand}"
                        Width="150" Height="40" Margin="5"
                        Background="#2ECC71" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#27AE60"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Restablecer Valores" Command="{Binding ResetSettingsCommand}"
                        Width="150" Height="40" Margin="5"
                        Background="#95A5A6" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#7F8C8D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
