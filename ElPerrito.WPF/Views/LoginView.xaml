<Window x:Class="ElPerrito.WPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElPerrito.WPF.Views"
        xmlns:vm="clr-namespace:ElPerrito.WPF.ViewModels"
        mc:Ignorable="d"
        Title="El Perrito - Login"
        Height="535" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="550" MinWidth="450"
        Background="#ECF0F1">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="0,0,0,12">
        <Border Background="White"
                CornerRadius="10"
                Margin="40,40,40,22"
                BorderBrush="#BDC3C7"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="315"
                                ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>

            <StackPanel Margin="40">
                <!-- Logo -->
                <Image Source="/Assets/logo.png"
                      Width="80" Height="80"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,15"
                      Stretch="Uniform"/>

                <!-- Título -->
                <TextBlock Text="El Perrito"
                          FontSize="32"
                          FontWeight="Bold"
                          Foreground="#2C3E50"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,5"/>


                <!-- Email -->
                <TextBlock Text="Correo electrónico"
                          FontSize="12"
                          FontWeight="Bold"
                          Foreground="#2C3E50"
                          Margin="0,0,0,5"/>

                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                        FontSize="14"
                        Padding="10"
                        BorderBrush="#BDC3C7"
                        BorderThickness="1"
                        Height="40"
                        Margin="0,0,0,20">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#3498DB"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Password -->
                <TextBlock Text="Contraseña"
                          FontSize="12"
                          FontWeight="Bold"
                          Foreground="#2C3E50"
                          Margin="0,0,0,5"/>

                <PasswordBox x:Name="PasswordBox"
                            FontSize="14"
                            Padding="10"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1"
                            Height="40"
                            Margin="0,0,0,10"
                            PasswordChanged="PasswordBox_PasswordChanged">
                    <PasswordBox.Style>
                        <Style TargetType="PasswordBox">
                            <Style.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#3498DB"/>
                                    <Setter Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </PasswordBox.Style>
                </PasswordBox>

                <!-- Mensaje de error -->
                <TextBlock Text="{Binding ErrorMessage}"
                          FontSize="12"
                          Foreground="#E74C3C"
                          TextWrapping="Wrap"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"
                          Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Botón Login -->

                <!-- Loading indicator -->
                <TextBlock Text="Iniciando sesión..."
                          FontSize="12"
                          Foreground="#7F8C8D"
                          HorizontalAlignment="Center"
                          Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <Button Content="INICIAR SESIÓN"
                       Command="{Binding LoginCommand}"
                       CommandParameter="{Binding ElementName=PasswordBox}"
                       FontSize="14"
                       FontWeight="Bold"
                       Foreground="White"
                       Background="#3498DB"
                       Height="46"
                       BorderThickness="0"
                       Cursor="Hand" 
                       Width="224"
                       Margin="0,20,0,0">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}"
                                               CornerRadius="5"
                                               BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2980B9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#21618C"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#BDC3C7"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>
