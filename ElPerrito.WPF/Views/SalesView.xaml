<UserControl x:Class="ElPerrito.WPF.Views.SalesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Estadísticas rápidas -->
        <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,15">
            <Border Background="#3498DB" CornerRadius="5" Padding="15" Margin="0,0,5,0">
                <StackPanel>
                    <TextBlock Text="Total Ventas Hoy" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock Text="{Binding TotalSalesToday, StringFormat=C2}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="#2ECC71" CornerRadius="5" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Ventas del Mes" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock Text="{Binding TotalSalesMonth, StringFormat=C2}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="#E67E22" CornerRadius="5" Padding="15" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Órdenes Pendientes" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock Text="{Binding PendingOrders}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="#9B59B6" CornerRadius="5" Padding="15" Margin="5,0,0,0">
                <StackPanel>
                    <TextBlock Text="Total Órdenes" Foreground="White" FontSize="12" Opacity="0.8"/>
                    <TextBlock Text="{Binding TotalOrders}" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Lista de ventas -->
        <Border Grid.Row="1" Background="White" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filtros -->
                <Border Grid.Row="0" Background="#F8F9FA" Padding="10" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Filtrar por fecha:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding FilterStartDate}" Width="120" Margin="0,0,10,0"/>
                        <TextBlock Text="a" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding FilterEndDate}" Width="120" Margin="0,0,10,0"/>
                        <Button Content="🔍 Aplicar" Command="{Binding ApplyFilterCommand}"
                                Width="80" Height="30" Margin="10,0,0,0"
                                Background="#3498DB" Foreground="White" FontSize="12" BorderThickness="0" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- DataGrid de ventas -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Sales}"
                          SelectedItem="{Binding SelectedSale}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F8F9FA"
                          RowHeight="40"
                          FontSize="13">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID Venta" Binding="{Binding IdVenta}" Width="80"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding NombreCliente}" Width="*"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C2}" Width="100"/>
                        <DataGridTextColumn Header="Estado Pago" Binding="{Binding EstadoPago}" Width="120"/>
                        <DataGridTemplateColumn Header="Acciones" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="👁️ Ver"
                                                Command="{Binding DataContext.ViewSaleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Width="70" Height="28" Margin="2"
                                                Background="#3498DB" Foreground="White"
                                                FontSize="11" BorderThickness="0" Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
